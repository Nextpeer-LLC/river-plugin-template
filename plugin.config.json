{
  "name": "river-plugin-template",
  "version": "1.0.0",
  "description": "Template plugin for River platform",
  "pricePerCall": 0.05,
  "functions": [
    {
      "name": "exampleFunction",
      "description": "Converts input to uppercase",
      "parameters": [
        {
          "name": "input",
          "type": "object",
          "description": "The input string to process",
          "required": true,
          "properties": {
            "text": { "type": "string" }
          }
        }
      ],
      "returns": {
        "type": "object",
        "properties": {
          "original": { "type": "string" },
          "processed": { "type": "string" },
          "timestamp": { "type": "string" }
        }
      }
    }
  ]
}
